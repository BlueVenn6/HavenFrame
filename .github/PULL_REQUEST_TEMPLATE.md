## Scope / 修改范围

Describe the user-visible behavior and the affected desktop/mobile/backend call path.

## Verification / 验证

- [ ] Relevant tests were run and their exact commands are listed below.
- [ ] Chinese and English UI remain consistent where display text changed.
- [ ] Provider IDs, model IDs, endpoints, and payload values were not localized.
- [ ] No API key, customer asset, database, output, log, signing file, or local path was committed.
- [ ] Final artifact claims distinguish source tests, build success, and installed-product validation.

Commands and results:

```text

```

## Provider and data impact / Provider 与数据影响

State whether this change sends data to a different service, changes a Provider request, changes persistence, or changes migrations. Write `None` when it does not.
