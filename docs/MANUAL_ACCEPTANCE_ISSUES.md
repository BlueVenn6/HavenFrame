# Windows 人工验收问题记录

记录规则：先稳定复现并登记，不在单次验收中直接触发大范围重构。修复顺序为 P0、P1、P2、P3。

严重程度：P0 数据损坏、安全或完全不可用；P1 核心功能无法使用；P2 功能可用但有明显错误；P3 视觉或体验问题。

| ID | 功能 | 操作步骤 | 预期结果 | 实际结果 | 严重程度 | 截图 | 是否稳定复现 | 根因 | 修复 commit | 验证结果 |
|---|---|---|---|---|---|---|---|---|---|---|
| QA-001 | 模型设置 / 工作流路由 | 保存带 Key 的新配置后发起平面图任务 | 新配置立即成为所选工作流的运行配置 | 任务仍读取旧的无 Key 配置并报 `API key is required` | P1 | 用户截图（任务 #1、#2） | 是 | “保存配置”和“应用到工作流”是两个独立动作，启动种子还会覆盖用户偏好 | 47c97aa | PASS：平面图任务 #5、#7 使用配置 #22/#23 真实生成成功；重启后 floorplan/boards/space_render 保持配置 #22 |
| QA-002 | OpenAI 兼容中转 | 仅 PATCH Base URL 后执行任务 | 只更新 Base URL，认证和 adapter 路由保持不变 | partial PATCH 把 compatibility、endpoint 和 extra config 重置为默认值 | P1 | 后端任务记录 #4 | 是 | 更新服务把局部 payload 当成完整配置重新规范化 | 47c97aa | PASS：新增 partial update 回归；全量 161 passed |
| QA-003 | 模型连接测试 | 使用不含 `/v1` 的中转地址点击测试连接 | 明确报告模型端点 404，不能声称线路可用 | 404 被标记为“中转可达/已跳过付费测试” | P1 | 用户截图 | 是 | 非付费探测错误地把 Base URL 首页可达等同于模型端点可用 | 47c97aa | PASS：404 现在为失败并报告实际 `/models` 端点；真实可用性由任务队列生成结果证明 |
| QA-004 | 空间渲染 | 使用 timeout=360 的已保存配置发起空间渲染 | 任务继承配置超时并完成真实生成 | 任务创建强制写入 240 秒，覆盖已保存配置 | P1 | 任务 #10 | 是 | task service 无条件注入默认超时 | 47c97aa | PASS：任务 #11 使用配置 #22、360 秒快照真实生成成功；资产 #8 重启后仍以 200 image/png 返回 |
| QA-005 | 多房间方案板 | 两张已完成 GLM 提取的图片生成综合方案板 | 生成综合板、预算板和两个分房间板 | 预算数据中的 `rooms` 为整数，预览器按数组切片导致 500 | P1 | backend.stderr.log / 原任务 #14 | 是 | 预览 renderer 未规范化结构化内容字段类型 | 47c97aa | PASS：任务 #15 成功，文档 #5-#8、预览资产 #13-#16 均可读取；新增双素材集成回归 |
| QA-006 | 任务队列状态 | 方案板预览生成异常 | 任务状态必须变为 failed 并保留错误 | 任务永久停留 queued，形成假进行中 | P1 | 任务 #14 | 是 | 同步方案板 composer 缺少异常状态收口 | 47c97aa | PASS：单房间/报价卡/多房间统一 running-success/failed 生命周期；QA 任务 #14 已保留并改为 failed |

状态建议：`PASS`、`FAIL`、`BLOCKED_BY_CREDENTIALS`、`BLOCKED_BY_ENVIRONMENT`、`NOT_RUN`。
